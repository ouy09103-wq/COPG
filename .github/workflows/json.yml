name: json

on:
  workflow_dispatch:
    inputs:
      upload_to_release:
        description: 'Upload ZIP to GitHub Release?'
        type: boolean
        required: true
        default: false
      upload_to_telegram:
        description: 'Upload ZIP to Telegram?'
        type: boolean
        required: true
        default: false
      branch:
        description: 'Branch to build from'
        type: string
        required: true
        default: 'rewrite'
      title:
        description: 'Custom title for Telegram message'
        type: string
        required: false
        default: ''
      custom_version:
        description: 'Custom version (e.g. 4.3.5)'
        type: string
        required: false
      custom_version_code:
        description: 'Custom version code (e.g. 435)'
        type: string
        required: false

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: ubuntu-24.04
    environment: Telegram

    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Set up Android SDK and NDK
        run: |
          echo "ANDROID_HOME=$HOME/android-sdk" >> $GITHUB_ENV
          mkdir -p $ANDROID_HOME/cmdline-tools
          wget https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdline-tools.zip
          unzip cmdline-tools.zip -d $ANDROID_HOME/cmdline-tools
          if [ -d "$ANDROID_HOME/cmdline-tools/cmdline-tools" ]; then
            mv $ANDROID_HOME/cmdline-tools/cmdline-tools $ANDROID_HOME/cmdline-tools/latest
          elif [ -d "$ANDROID_HOME/cmdline-tools/tools" ]; then
            mv $ANDROID_HOME/cmdline-tools/tools $ANDROID_HOME/cmdline-tools/latest
          else
            echo "Unexpected directory structure after unzip:"
            ls -la $ANDROID_HOME/cmdline-tools
            exit 1
          fi
          echo "$ANDROID_HOME/cmdline-tools/latest/bin" >> $GITHUB_PATH
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-34" "build-tools;34.0.0"
          mkdir -p $ANDROID_HOME/ndk
          wget https://dl.google.com/android/repository/android-ndk-r29-linux.zip -O ndk.zip
          unzip -q ndk.zip -d $ANDROID_HOME/ndk
          NDK_DIR=$(find $ANDROID_HOME/ndk -maxdepth 1 -type d -name "android-ndk-r29*" | head -n 1)
          echo "NDK_PATH=$NDK_DIR" >> $GITHUB_ENV
          echo "Installed NDK at $NDK_DIR"

      - name: Install build tools and Python dependencies
        run: |
          sudo apt-get purge -y man-db 2>/dev/null || true
          sudo rm -rf /var/cache/man /var/lib/man-db /usr/share/man 2>/dev/null || true
          echo 'DPkg::Post-Invoke { ":"; };' | sudo tee /etc/apt/apt.conf.d/99noman >/dev/null
          sudo apt-get update
          sudo apt-get install -y build-essential cmake zip curl imagemagick python3 python3-pip upx-ucl
          pip3 install telethon telegraph requests

      - name: Build Unified Controller (All Architectures)
        run: |
          echo "Building ARM64..."
          ${{ env.NDK_PATH }}/toolchains/llvm/prebuilt/linux-x86_64/bin/aarch64-linux-android34-clang++ \
            -std=c++17 -O3 -Os -fPIE -pie -static-libstdc++ \
            -fdata-sections -ffunction-sections -Wl,--gc-sections \
            -I src/include src/unified_controller.cpp -o controller_arm64
          
          echo "Building ARMv7..."
          ${{ env.NDK_PATH }}/toolchains/llvm/prebuilt/linux-x86_64/bin/armv7a-linux-androideabi34-clang++ \
            -std=c++17 -O3 -Os -fPIE -pie -static-libstdc++ \
            -fdata-sections -ffunction-sections -Wl,--gc-sections \
            -I src/include src/unified_controller.cpp -o controller_armv7
          
          echo "Building x86_64..."
          ${{ env.NDK_PATH }}/toolchains/llvm/prebuilt/linux-x86_64/bin/x86_64-linux-android34-clang++ \
            -std=c++17 -O3 -Os -fPIE -pie -static-libstdc++ \
            -fdata-sections -ffunction-sections -Wl,--gc-sections \
            -I src/include src/unified_controller.cpp -o controller_x86_64
          
          echo "Building x86..."
          ${{ env.NDK_PATH }}/toolchains/llvm/prebuilt/linux-x86_64/bin/i686-linux-android34-clang++ \
            -std=c++17 -O3 -Os -fPIE -pie -static-libstdc++ \
            -fdata-sections -ffunction-sections -Wl,--gc-sections \
            -I src/include src/unified_controller.cpp -o controller_x86

          echo "Stripping and compressing binaries..."
          ${{ env.NDK_PATH }}/toolchains/llvm/prebuilt/linux-x86_64/bin/llvm-strip --strip-unneeded controller_*
          upx --best --lzma -q controller_* 2>/dev/null || echo "UPX optimization skipped for some binaries"

          echo "Controller built successfully:"
          ls -lh controller_*

      - name: Build Project (arm64-v8a)
        run: |
          mkdir -p build/arm64-v8a
          cd src
          cmake -S . -B ../build/arm64-v8a \
            -DCMAKE_TOOLCHAIN_FILE=${{ env.NDK_PATH }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=arm64-v8a \
            -DANDROID_PLATFORM=android-34 \
            -DCMAKE_MAKE_PROGRAM=/usr/bin/make
          cmake --build ../build/arm64-v8a

      - name: Build Project (armeabi-v7a)
        run: |
          mkdir -p build/armeabi-v7a
          cd src
          cmake -S . -B ../build/armeabi-v7a \
            -DCMAKE_TOOLCHAIN_FILE=${{ env.NDK_PATH }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=armeabi-v7a \
            -DANDROID_PLATFORM=android-34 \
            -DCMAKE_MAKE_PROGRAM=/usr/bin/make
          cmake --build ../build/armeabi-v7a

      - name: Build Project (x86_64)
        run: |
          mkdir -p build/x86_64
          cd src
          cmake -S . -B ../build/x86_64 \
            -DCMAKE_TOOLCHAIN_FILE=${{ env.NDK_PATH }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=x86_64 \
            -DANDROID_PLATFORM=android-34 \
            -DCMAKE_MAKE_PROGRAM=/usr/bin/make
          cmake --build ../build/x86_64

      - name: Build Project (x86)
        run: |
          mkdir -p build/x86
          cd src
          cmake -S . -B ../build/x86 \
            -DCMAKE_TOOLCHAIN_FILE=${{ env.NDK_PATH }}/build/cmake/android.toolchain.cmake \
            -DANDROID_ABI=x86 \
            -DANDROID_PLATFORM=android-34 \
            -DCMAKE_MAKE_PROGRAM=/usr/bin/make
          cmake --build ../build/x86

      - name: Prepare module structure
        run: |
          mkdir -p module/zygisk module/webroot module/common

          # Zygisk Libraries
          cp build/arm64-v8a/libspoof.so module/zygisk/arm64-v8a.so
          cp build/armeabi-v7a/libspoof.so module/zygisk/armeabi-v7a.so
          cp build/x86_64/libspoof.so module/zygisk/x86_64.so
          cp build/x86/libspoof.so module/zygisk/x86.so
          chmod 755 module/zygisk/*.so

          # Controllers
          cp controller_arm64 module/controller_arm64
          cp controller_armv7 module/controller_armv7
          cp controller_x86_64 module/controller_x86_64
          cp controller_x86 module/controller_x86
          chmod 755 module/controller_*

          # Other files (COPG.json, list.json, META-INF already live under module/)
          # No system/ — COPG is fully systemless (no /system mount, no metamodule).
          cp -r webroot/* module/webroot/ 2>/dev/null || true

          chmod 755 module/customize.sh module/service.sh module/uninstall.sh module/META-INF/com/google/android/update-binary

          VERSION="${{ github.event.inputs.custom_version || '5.3.1' }}"
          VERSION_CODE="${{ github.event.inputs.custom_version_code || '531' }}"
          cat > module/module.prop << EOF
          id=COPG
          name=✨ COPG SPOOF ✨
          version=$VERSION
          versionCode=$VERSION_CODE
          author=AlirezaParsi
          description=Spoof your device for games/apps to unlock new features.
          support=https://t.me/theaosp
          updateJson=https://raw.githubusercontent.com/AlirezaParsi/COPG/refs/heads/JSON/update.json
          minMagisk=20.4
          banner=banner.png
          EOF

      - name: Package module into ZIP
        run: |
          VERSION=$(grep "version=" module/module.prop | cut -d'=' -f2)
          ZIP_NAME="COPG-v$VERSION.zip"
          cd module
          zip -r "../$ZIP_NAME" .
          cd ..
          echo "ZIP_NAME=$ZIP_NAME" >> $GITHUB_ENV
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Extract changelog for current version
        if: github.event.inputs.upload_to_telegram == 'true'
        run: |
          if [ -f "changelog.md" ]; then
            VERSION="${{ env.VERSION }}"
            CHANGELOG_CONTENT=$(sed -n "/^## v${VERSION}/,/^## v/p" changelog.md | sed '$d')
            if [ -n "$CHANGELOG_CONTENT" ]; then
              CLEAN_CHANGELOG=$(echo "$CHANGELOG_CONTENT" | sed '1d')
              if [ -n "$CLEAN_CHANGELOG" ]; then
                echo "CHANGELOG<<EOF" >> $GITHUB_ENV
                echo "$CLEAN_CHANGELOG" >> $GITHUB_ENV
                echo "EOF" >> $GITHUB_ENV
              else
                echo "CHANGELOG=No changelog body found for v${VERSION}" >> $GITHUB_ENV
              fi
            else
              echo "CHANGELOG=No changelog found for v${VERSION}" >> $GITHUB_ENV
            fi
          else
            echo "CHANGELOG=No changelog file available" >> $GITHUB_ENV
          fi

      - name: Download and Prepare Thumbnail
        if: github.event.inputs.upload_to_telegram == 'true'
        run: |
          # Flatten the icon's transparent rounded corners onto the tile's dark colour and
          # crop to an edge-to-edge square — JPEG has no alpha, so without this the margin
          # would render as a white border in the Telegram thumbnail.
          convert webroot/icon.png -background '#12181E' -alpha remove -alpha off \
            -resize 320x320^ -gravity center -extent 320x320 -strip -quality 90 icon_thumb.jpg
          if [ ! -f "icon_thumb.jpg" ]; then
            echo "Error: Thumbnail creation failed!"
            ls -la
            exit 1
          fi

      - name: Upload Module ZIP as Artifact
        uses: actions/upload-artifact@v7
        with:
          name: COPG
          path: ${{ env.ZIP_NAME }}

      - name: Upload ZIP to GitHub Release
        if: github.event.inputs.upload_to_release == 'true'
        run: |
          TAG="v${{ env.VERSION }}"
          
          echo "📝 Preparing release notes for v${{ env.VERSION }}..."
          
          RELEASE_NOTES="## COPG v${{ env.VERSION }}\n\n"
          
          if [ -f "changelog.md" ]; then
            CHANGELOG_SECTION=$(awk -v version="${{ env.VERSION }}" '
              $0 ~ "^## v" version {
                found=1
                next
              }
              found && /^## v/ {
                found=0
                next
              }
              found {
                print
              }
            ' changelog.md)
            
            if [ -n "$CHANGELOG_SECTION" ]; then
              RELEASE_NOTES+="$CHANGELOG_SECTION"
            else
              RELEASE_NOTES+="No changelog found for this version.\n\n"
            fi
          else
            RELEASE_NOTES+="No changelog file available.\n\n"
          fi
          
          RELEASE_NOTES+="\n---\n"
          RELEASE_NOTES+="**Build Date**: $(date -u +'%Y-%m-%d %H:%M:%S UTC')\n"
          RELEASE_NOTES+="**Branch**: ${{ github.event.inputs.branch }}\n"
          RELEASE_NOTES+="**Commit**: ${{ github.sha }}\n\n"
          RELEASE_NOTES+="Read the full changelog for all versions in [changelog.md](https://github.com/${{ github.repository }}/blob/${{ github.event.inputs.branch }}/changelog.md)"
          
          echo -e "$RELEASE_NOTES" > release_notes.txt

          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists — updating notes + re-uploading asset..."
            gh release edit "$TAG" \
              --title "COPG v${{ env.VERSION }}" \
              --notes-file release_notes.txt
            gh release upload "$TAG" ${{ env.ZIP_NAME }} --clobber
          else
            echo "Creating release $TAG..."
            gh release create "$TAG" \
              --title "COPG v${{ env.VERSION }}" \
              --notes-file release_notes.txt \
              ${{ env.ZIP_NAME }}
          fi

          echo "✅ Release published successfully!"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update update.json in JSON branch
        if: github.event.inputs.upload_to_release == 'true'
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
    
          VERSION="${{ env.VERSION }}"
          VERSION_CODE="${{ env.VERSION_CODE || env.VERSION }}"
          ZIP_URL="https://github.com/${{ github.repository }}/releases/download/v${VERSION}/${{ env.ZIP_NAME }}"
          CHANGELOG_URL="https://raw.githubusercontent.com/${{ github.repository }}/${{ github.event.inputs.branch }}/changelog.md"
    
          cat > update.json << EOF
          {
            "version": "v${VERSION}",
            "zipUrl": "${ZIP_URL}",
            "versionCode": "${VERSION_CODE//./}",
            "changelog": "${CHANGELOG_URL}"
          }
          EOF
          
          git fetch origin
          git checkout JSON
          git pull origin JSON
    
          mv -f update.json update.json.new 2>/dev/null || true
          cp update.json.new update.json 2>/dev/null || cp update.json update.json
          rm -f update.json.new
    
          git add update.json
          git commit -m "Update update.json to v${VERSION}" || echo "No changes to update.json"
          git push origin JSON
    
          echo "✅ update.json updated successfully in JSON branch"

      - name: Create Telegram Notification Script
        if: github.event.inputs.upload_to_telegram == 'true'
        run: |
          cat > send_telegram.py << 'PYEOF'
          from telethon import TelegramClient
          from telegraph import Telegraph
          import os
          import asyncio
          import re
          
          async def send_notification():
              try:
                  bot_token = os.getenv('TELEGRAM_TOKEN')
                  chat_id = int(os.getenv('TELEGRAM_CHAT_ID'))
                  zip_name = os.getenv('ZIP_NAME')
                  title = os.getenv('TITLE')
                  version = os.getenv('VERSION')
                  branch = os.getenv('BRANCH')
                  commit = os.getenv('COMMIT')[:8]
                  workflow_url = os.getenv('WORKFLOW_RUN_URL')
                  changelog_raw = os.getenv('CHANGELOG', '').strip()
                  is_release = os.getenv('UPLOAD_TO_RELEASE') == 'true'
                  
                  client = TelegramClient('github_bot', 2040, 'b18441a1ff607e10a989891a5462e627')
                  await client.start(bot_token=bot_token)
                  
                  build_type = "🚀 **Release Build**" if is_release else "🔧 **CI Build**"
                  message = (
                      f"✨ **{title}** ✨\n\n"
                      f"{build_type}\n"
                      f"▫️ **Version**: v{version}\n"
                      f"▫️ **Branch**: {branch}\n"
                      f"▫️ **Commit**: {commit}\n\n"
                  )
                  
                  telegraph_url = ""
                  if is_release and changelog_raw and not changelog_raw.startswith("No changelog"):
                      telegraph = Telegraph()
                      telegraph.create_account(short_name='COPG-Bot')
                      content_html = f"<h3>Changelog for COPG v{version}</h3>"
                      lines = changelog_raw.split('\n')
                      in_list = False
                      for line in lines:
                          line = line.strip()
                          if not line: 
                              if in_list:
                                  content_html += "</ul>"
                                  in_list = False
                              continue
                          if line.startswith('###'):
                              if in_list:
                                  content_html += "</ul>"
                                  in_list = False
                              content_html += f"<h4>{line[3:].strip()}</h4>"
                          elif line.startswith('*'):
                              if not in_list:
                                  content_html += "<ul>"
                                  in_list = True
                              list_item = line[1:].strip()
                              # Stash code spans FIRST so any * inside them (e.g. `Build.*`,
                              # `ro.*`) is not paired by the emphasis regexes — otherwise a
                              # lone * inside a backtick span gets matched as italic across a
                              # </code>/<b> boundary and Telegraph rejects the overlapping tags.
                              code_spans = []
                              def _stash(m):
                                  code_spans.append(m.group(1))
                                  return f"\x00{len(code_spans) - 1}\x00"
                              list_item = re.sub(r'`(.*?)`', _stash, list_item)
                              list_item = re.sub(r'\*\*(.*?)\*\*', r'<b>\1</b>', list_item)
                              list_item = re.sub(r'\*(.*?)\*', r'<i>\1</i>', list_item)
                              list_item = re.sub(r'\x00(\d+)\x00', lambda m: f"<code>{code_spans[int(m.group(1))]}</code>", list_item)
                              content_html += f"<li>{list_item}</li>"
                          else:
                              if in_list:
                                  content_html += "</ul>"
                                  in_list = False
                              content_html += f"<p>{line}</p>"
                      if in_list:
                          content_html += "</ul>"
                      response = telegraph.create_page(
                          title=f'COPG v{version} Changelog',
                          author_name='COPG Build Bot',
                          author_url='https://github.com/AlirezaParsi/COPG',
                          html_content=content_html
                      )
                      telegraph_url = f"https://telegra.ph/{response['path']}"
                      message += f"📋 **[Full Changelog]({telegraph_url})**\n\n"
                  
                  message += f"🔗 [View Workflow Run]({workflow_url})"
                  
                  await client.send_file(
                      chat_id,
                      file=zip_name,
                      caption=message,
                      thumb=os.getenv('THUMBNAIL_PATH'),
                      parse_mode='markdown'
                  )
                  print(f"✅ Message sent successfully!")
                  if telegraph_url:
                      print(f"📄 Changelog published at: {telegraph_url}")
                  await client.disconnect()
              except Exception as e:
                  print(f"❌ Failed to send notification: {e}")
                  exit(1)
          
          asyncio.run(send_notification())
          PYEOF

      - name: Send Telegram Notification
        if: github.event.inputs.upload_to_telegram == 'true'
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          ZIP_NAME: ${{ env.ZIP_NAME }}
          THUMBNAIL_PATH: icon_thumb.jpg
          TITLE: ${{ github.event.inputs.title || 'New COPG Spoof Module Build' }}
          WORKFLOW_RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          UPLOAD_TO_RELEASE: ${{ github.event.inputs.upload_to_release }}
          VERSION: ${{ env.VERSION }}
          BRANCH: ${{ github.event.inputs.branch }}
          COMMIT: ${{ github.sha }}
          CHANGELOG: ${{ env.CHANGELOG }}
        run: |
          python3 send_telegram.py
